---
apiVersion: v1alpha1
kind: UserVolumeConfig
name: longhorn
provisioning:
  diskSelector:
    match: disk.transport == 'nvme'
  minSize: 340GB
  # maxSize: 360GB
  grow: true
filesystem:
  type: xfs
