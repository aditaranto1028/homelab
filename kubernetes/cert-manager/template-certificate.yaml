---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: <SERVICE>-certificate
  namespace: <NAMESPACE>
spec:
  secretName: <SERVICE>-certificate-secret
  issuerRef:
    name: cloudflare-clusterissuer
    kind: ClusterIssuer
  dnsNames:
    - <SERVICE>.ditaranto-homelab.com
