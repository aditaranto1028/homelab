---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ntfy-certificate
  namespace: ntfy
spec:
  secretName: ntfy-certificate-secret
  issuerRef:
    name: cloudflare-clusterissuer
    kind: ClusterIssuer
  dnsNames:
    - ntfy.ditaranto-homelab.com
